# 播放列表自动切换功能 - 测试清单

## Phase 4: 测试验证

### 1. Web 端功能测试

#### 1.1 播放列表变更检测
- [ ] WebSocket 连接成功建立
- [ ] 收到 `playlist_assigned` 消息时触发下载
- [ ] 收到 `playlist_updated` 消息时检查版本
- [ ] WebSocket 断开时自动重连
- [ ] 心跳版本比对作为兜底机制

#### 1.2 下载功能
- [ ] 开始下载时状态变为 `downloading`
- [ ] 下载进度正确更新
- [ ] 下载完成时状态变为 `completed`
- [ ] 下载失败时自动重试（最多3次）
- [ ] 取消下载功能正常工作
- [ ] 已缓存的文件跳过下载

#### 1.3 自动切换
- [ ] 下载完成后自动切换播放列表
- [ ] 切换前暂停当前播放
- [ ] 切换后重置播放索引为0
- [ ] 切换完成通知后端
- [ ] UI 显示切换进度

#### 1.4 失败回滚
- [ ] 切换失败时恢复原播放列表
- [ ] 显示错误信息
- [ ] 允许手动重试

### 2. Android 端功能测试

#### 2.1 JsBridge 接口
- [ ] `startPlaylistDownload` 正确调用
- [ ] `getPlaylistCacheStatus` 返回正确状态
- [ ] `cancelPlaylistDownload` 取消下载
- [ ] `reportSwitchComplete` 通知完成

#### 2.2 下载功能
- [ ] Android DownloadManager 正确下载
- [ ] 下载进度回调正确
- [ ] 下载完成回调正确
- [ ] 下载错误回调正确

### 3. 网络异常场景测试

#### 3.1 断网重连
- [ ] 断网时下载暂停
- [ ] 网络恢复后继续下载
- [ ] 部分下载的文件不丢失

#### 3.2 下载中断恢复
- [ ] 关闭浏览器后状态持久化
- [ ] 重新打开后恢复下载状态
- [ ] 已完成文件不重复下载

#### 3.3 服务器错误
- [ ] 500 错误时重试
- [ ] 404 错误时标记失败
- [ ] 超时时重试

### 4. 边界条件测试

#### 4.1 空播放列表
- [ ] 正确处理空播放列表
- [ ] 不触发下载
- [ ] 直接切换

#### 4.2 大型播放列表
- [ ] 100+ 文件时并发控制（最多3个）
- [ ] 内存使用稳定
- [ ] UI 不卡顿

#### 4.3 特殊字符处理
- [ ] 文件名包含中文
- [ ] 文件名包含特殊字符
- [ ] URL 包含查询参数

### 5. 性能测试

#### 5.1 下载性能
- [ ] 并发下载正常工作
- [ ] 下载速度合理
- [ ] 不阻塞主线程

#### 5.2 存储性能
- [ ] IndexedDB 读写正常
- [ ] Cache API 存取正常
- [ ] 存储空间检查

### 6. UI/UX 测试

#### 6.1 状态显示
- [ ] 下载进度条显示
- [ ] 切换状态提示
- [ ] 错误信息显示

#### 6.2 交互
- [ ] 可取消下载
- [ ] 可确认切换
- [ ] 可回滚

---

## 自动化测试结果

```
✓ src/player/__tests__/playlistSwitch.test.ts (11 tests) 61ms

Test Files  1 passed (1)
Tests       11 passed (11)
```

### 测试覆盖
- PlaylistDownloadManager 模块导出验证
- usePlaylistDownload Hook 导出验证
- usePlaylistSwitch Hook 导出验证
- usePlaylistChangeDetection Hook 导出验证
- Types 模块导入验证

---

## 后续改进建议

1. **集成测试**: 使用 Playwright 进行端到端测试
2. **压力测试**: 模拟 1000+ 文件的播放列表
3. **弱网测试**: 模拟 2G/3G 网络环境
4. **兼容性测试**: 测试不同浏览器版本
